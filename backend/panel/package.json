{
  "name": "@chessbot/panel",
  "version": "0.1.0",
  "private": true,
  "description": "Chessbot local dashboard panel (Vite-built static app).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node -e \"console.log('panel has no start script; use \\\"npm run dev --workspace @chessbot/panel\\\" or \\\"npm start\\\" from the repo root')\"",
    "typecheck": "echo no typecheck for panel"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
