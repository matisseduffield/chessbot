{
  "_comment": "Phase 2 typing scaffold. We don't emit anything (the build is Vite); this config exists so `tsc --noEmit` can type-check JavaScript that opts in via `// @ts-check`. checkJs is FALSE — only files with the directive are checked, so we can grow coverage one file at a time without surfacing thousands of warnings on day one.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "resolveJsonModule": true,
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": []
  },
  "include": ["src/**/*.js", "src/**/*.jsx"],
  "exclude": ["node_modules", "dist", "**/*.test.js"]
}
